# @emela/design-system

Source unique du style emela : tokens, base typographique, composants `.em-*`.

```js
import '@emela/design-system/index.css';
```

Ne jamais coder une couleur/taille en dur ailleurs — toujours via les variables `--*` de `tokens.css`.
